{
  "title": "T88R Public Research Actor input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Mode",
      "type": "string",
      "enum": ["research"],
      "default": "research",
      "description": "Research-only mode. This Actor has no sales, Telegram, messaging, negotiation, or handoff mode."
    },
    "markets": {
      "title": "Markets",
      "type": "array",
      "description": "Public-source markets to label and filter.",
      "editor": "stringList",
      "default": ["Malaysia", "Singapore"],
      "items": {
        "type": "string"
      }
    },
    "targetCompanies": {
      "title": "Target companies",
      "type": "array",
      "description": "Named hyperscalers and infrastructure operators to research.",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "maxItems": 50
    },
    "startUrls": {
      "title": "Public source URLs",
      "type": "array",
      "description": "Public business pages only. Do not provide login URLs, private profiles, personal contact pages, or authenticated session URLs.",
      "editor": "requestListSources",
      "prefill": [
        {
          "url": "https://"
        }
      ],
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "title": "Public source URL",
            "description": "HTTPS URL of a public source to inspect.",
            "type": "string",
            "pattern": "^https://"
          }
        },
        "required": ["url"]
      }
    },
    "locations": {
      "title": "Locations",
      "type": "array",
      "description": "Malaysia and Singapore location labels for evidence classification.",
      "editor": "stringList",
      "default": [
        "Johor",
        "Negeri Sembilan",
        "Selangor",
        "Kuala Lumpur",
        "Cyberjaya",
        "Singapore"
      ],
      "items": {
        "type": "string"
      },
      "maxItems": 20
    },
    "maxPages": {
      "title": "Maximum pages",
      "type": "integer",
      "description": "Hard page ceiling for the pilot.",
      "default": 20,
      "minimum": 1,
      "maximum": 20
    },
    "maxItems": {
      "title": "Maximum dataset items",
      "type": "integer",
      "description": "Hard output-item ceiling for the pilot.",
      "default": 50,
      "minimum": 1,
      "maximum": 50
    },
    "maxDepth": {
      "title": "Maximum link depth",
      "type": "integer",
      "description": "Only direct source pages are supported in the pilot.",
      "default": 1,
      "minimum": 0,
      "maximum": 1
    },
    "timeoutSeconds": {
      "title": "Timeout seconds",
      "type": "integer",
      "description": "Run timeout ceiling for the pilot.",
      "default": 120,
      "minimum": 10,
      "maximum": 120
    },
    "tailMarker": {
      "title": "Run tail marker",
      "type": "string",
      "description": "Operator-supplied ISO timestamp marker copied into every output record."
      "editor": "textfield"
    }
  },
  "required": [
    "mode",
    "targetCompanies",
    "startUrls",
    "tailMarker"
  ]
}
